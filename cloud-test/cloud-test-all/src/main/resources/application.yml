server:
  port: 1122
  tomcat:
    # tomcat最大线程数，默认为200
    max-threads: 1
    # tomcat最大连接数,默认1000.实际连接数过大时会拒绝请求
    max-connections: 2
    connection-timeout: 1
  netty:
    connection-timeout: 1

spring:
  application:
    name: cloud-test-spring
#  boot:
#    enableautoconfiguration: false

test:
  name: sam




